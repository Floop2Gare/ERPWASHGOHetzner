# Variables d'environnement Backend (exemples, sans valeurs)
# Copier en .env sur le serveur: /srv/erp/BACK-END-ERP/.env

# Base de données Postgres (self-hosted)
DATABASE_URL=

# Emails / OAuth (si utilisés)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_REDIRECT_URI=

# Divers
ENABLE_DEBUG_ROUTES=false
ALLOWED_ORIGINS=https://votre-domaine.tld,https://www.votre-domaine.tld

# Legacy (désactivé)
# Les variables Supabase sont supprimées. Si vous deviez réactiver Supabase temporairement,
# décommentez et configurez les variables ci-dessous (non recommandé):
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_KEY=
# SUPABASE_JWT_SECRET=
